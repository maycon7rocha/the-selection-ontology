PREFIX selection: <http://www.iff.edu.br/schemas/selection#>
PREFIX owl: <http://www.w3.org/2002/07/owl#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
/*** Prefixos das ontologias específicas ***/

construct
{
    ?condition selection:class ?class .
    ?class a owl:Class;
                owl:equivalentClass 
                [
        	   /*** código OWL com as "Condições Necessárias e Suficientes" (critério) para selecionar os recursos ***/		
    		].
    
}
where
{
    ?condition a /***subclasse de selection:SimpleCondition***/ .
    BIND(URI(CONCAT(STR(?condition),"Class")) AS ?class)

    /*** 
    ?condition <propriedade mandatória> ?xxx .
    Se necessário, usar BIND assim: 
    BIND("String com o código de uma classe OWL, em sintaxe Turtle Pretty Printing, geralmente uma owl:Restriction usando CONCAT(...) e STR(?xxx). 
          Se envolver valores literais entre aspas, use aspas simples.") AS ?nomeDaClasseXXX) 
    ***/
    
    /***
    optional {?condition <propriedade opcional> ?yyy .}
    BIND(IF(BOUND(?yyy),
    "String com o código de uma classe OWL, em sintaxe Turtle Pretty Printing, geralmente uma owl:Restriction usando CONCAT(...) e STR(?xxx)" , 
    "Classe OWL, geralmente ou owl:Thing, quando a classe for usada em owl:intersectionOf, ou owl:Nothing, quando a classe for usada em owl:unionOf") AS ?nomeDaClasseYYY) 
    ***/  
}